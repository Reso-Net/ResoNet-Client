# Things to do
- Desktop notifications
- Pull webp version instead of weird shit from asset variation server
- User statistics
- Store generic settings in users inventory
- Don't fetch user profile on load but instead load on selection(Can't really do this unless I cache peoples pfps which I don't really want to do atm)
- Show a status that the user read a message from you
